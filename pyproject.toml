[tool.ruff]
line-length = 110

[tool.ruff.lint]
# Keep this to real bugs (unused imports/variables, undefined names, syntax
# issues) rather than style opinions - CI should fail on mistakes, not on
# nitpicks like import ordering or catching a broad Exception on purpose.
select = ["E4", "E7", "E9", "F"]
